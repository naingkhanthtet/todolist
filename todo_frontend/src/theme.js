import { createTheme } from "@mui/material/styles";

const theme = createTheme({
    palette: {
        mode: "dark",
        // primary: {
        //     main: "#000000",
        // },
        // secondary: {
        //     main: "#ece2ce",
        // },
    },
});

export default theme;
